# Copy to .env.local and fill with your Supabase project values
# (Supabase dashboard → Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Base URL of this app (used for OAuth redirects)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google Maps JavaScript API (optional — falls back to embed iframe without it)
NEXT_PUBLIC_GMAPS_KEY=your-google-maps-api-key
